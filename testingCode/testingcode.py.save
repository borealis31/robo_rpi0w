#!/usr/bin/python

from evdev import InputDevice, categorize, ecodes
from gpiozero import Motor
import time

remote = InputDevice('/dev/input/event0')

tM = Motor(27,17)

for event in remote.read_loop():
	if event.type == ecodes.EV_KEY:
		print(event.code)
		print(event.value)
	if event.code == 128:
		break
#	if event.code == 3 and (event.value == 1 or event.value == 2):#		tM.forward(1)
#	elif event.code == 3 and event.value == 0:
#		tM.forward(0)
#tM.forward(1)
#time.sleep(2)
#tM.forward(0)
